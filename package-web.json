{
  "name": "webadb-mirror",
  "version": "1.0.0",
  "description": "Browser-based phone mirroring using WebADB",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@yume-chan/adb": "^0.0.23",
    "@yume-chan/adb-backend-webusb": "^0.0.23",
    "@yume-chan/scrcpy": "^0.0.23",
    "@yume-chan/scrcpy-decoder-webcodecs": "^0.0.23",
    "@yume-chan/stream-extra": "^0.0.23"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}